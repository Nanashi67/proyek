# proyek

#fungsi+tampilan
print "--- kalkulator ---"
def pptq_am():
	A = input("")
	B = input("")
	P = raw_input("")
#processing
if P == "+":
	print "%d + %d = %d" % (a, b)
if P == "-":
	print "%d - %d = %d" % (a, b)
if P == "×":
	print "%d * %d = %d" % (a, b)
if P == "÷":
	print "%d / %d = %d" % (a, b)
else:
	error
pptq_am()
